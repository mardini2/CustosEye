# =========================
# CustosEye Requirements
# Compatible with Python 3.11.9
# =========================
# goal: Python package dependencies for CustosEye. includes runtime libraries for the
#       dashboard, monitoring agents, trust engine, and build tools. pin versions for
#       reproducible builds and deployments.

# --- Core runtime ---
# web framework for the dashboard
flask==3.0.0
# production-ready WSGI server (alternative to Flask dev server)
waitress==3.0.2
# cross-platform process and system monitoring (used by agents)
psutil==5.9.8
# executable builder for creating Windows .exe files
pyinstaller==6.6.0
# system tray icon support (optional feature)
pystray==0.19.5
# image processing library (required by pystray for tray icons)
pillow==10.3.0
# Excel file generation for export functionality
openpyxl==3.1.5

# --- Optional utilities ---
# HTTP library (used by algorithm module and for integrity scanning)
requests==2.32.3

# --- Development & Testing ---
# test framework for running unit tests
pytest==8.2.0
# code coverage plugin for pytest
pytest-cov==5.0.0
# code formatter (Black)
black==24.8.0
# fast Python linter (replaces Flake8 and more)
ruff==0.6.8
# static type checker for Python
mypy==1.11.1
# type stubs for requests library (helps mypy understand requests)
types-requests==2.32.0.20240622
# timeout plugin for pytest (prevents tests from hanging)
pytest-timeout==2.3.1

# --- System info (optional, used in builds or diagnostics) ---
# cross-platform directory utilities (used by PyInstaller and diagnostics)
platformdirs==4.2.0

# --- Notes ---
# Requires: Python 3.11.9
# To install everything:
#   python -m pip install -U pip
#   python -m pip install -r requirements.txt